import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mrgladiator14.screenmonitor',
  appName: 'screen-monitor',
  webDir: 'dist'
};

export default config;
